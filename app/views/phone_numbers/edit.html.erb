<h3>Edit <%= @phone_number.label %> Phone Number for <%= linked_contacts_for(@phone_number) %></h3>

<% semantic_form_for @phone_number, :url => phone_number_path(@phone_number) do |form| %>
  <%= render :partial => 'form', :object => form %>
<% end %>

<%= render :partial => 'navigation' %>